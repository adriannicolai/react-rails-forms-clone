import { combineReducers } from "redux";

const formsReducer = () => {
    return {};
}

const usersReducer = () => {
    return {};
}

export default combineReducers({
    forms: formsReducer,
    users: usersReducer
});